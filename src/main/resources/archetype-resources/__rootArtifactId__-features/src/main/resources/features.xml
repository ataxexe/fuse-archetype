#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<features name="${package}.${rootArtifactId}"
          xmlns="http://karaf.apache.org/xmlns/features/v1.0.0">
  
  <feature name="${rootArtifactId}" version="${project.version}">
    <bundle>mvn:${project.groupId}/${rootArtifactId}/${project.version}</bundle>
  </feature>

</features>
